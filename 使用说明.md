# 内存释放任务栏工具 v3.15.3 使用说明

## 1. 工具用途

这是面向 Windows 11 x64 的本地内存状态与清理工具。它可显示内存、网络状态，手动或定时整理后台进程工作集，并在符合配置时关闭明确列入清理名单的后台组件。

v3.15 修复了“清理实际完成，但界面等待到 120 秒后提示超时”的问题：清理 worker 的标准输出与错误输出会从启动时持续读取，不再因输出管道填满而互相等待。

v3.15.1 修复了嵌入任务栏时修改背景颜色并保存后，视觉刷新被误判为“拖出任务栏”、从而切换成悬浮窗口的问题。

v3.15.2 修复了旧安装器部署模块失败后仍创建快捷方式、重复安装另一份 Windhawk，以及旧 1.4.0 任务栏模块残留导致的双向拖放和背景颜色同步失效。新安装器会优先复用已在运行的 Portable Windhawk（例如 `D:\Windhawk`），并强制校验已安装模块为 1.6.2。

v3.15.3 将安装后的主程序和所有快捷方式统一为稳定文件名 `内存释放任务栏工具.exe`。后续升级只替换稳定入口，不会再因版本号变化导致桌面快捷方式空白或双击报错。

## 2. 支持环境

- Windows 11 x64。
- 不要求电脑具有 D: 盘；安装器会优先复用现有 Portable Windhawk，没有时才部署到当前用户的本地应用数据目录。
- 不需要开发环境、Git 或 PowerShell 7。
- 安装包内置经过签名和 SHA256 校验的 Windhawk 官方安装程序；首次加载当前 Windows 版本的任务栏模块时仍可能联网解析系统符号。
- ExplorerPatcher、StartAllBack 等任务栏替换软件可能使原生任务栏插槽不可用。

## 3. 安装包与安装后文件

- `MemoryCleanerFloat-Setup-v3.15.3.exe`：对外发送的唯一安装文件，内含本工具、Windhawk 官方安装程序和任务栏模块。
- `Uninstall.exe`：安装后生成的标准卸载入口，并注册到 Windows“已安装的应用”。

- `内存释放任务栏工具.exe`：稳定的双击启动入口，也是桌面和开始菜单快捷方式的固定目标。
- `内存释放任务栏工具.exe.config`：主程序运行配置，必须与 EXE 同目录。
- `MemoryCleanerFloat.ps1`：隐藏运行的一次性清理 worker，必须与 EXE 同目录；请勿单独复制 EXE。
- `MemoryCleanerTaskbarHost.exe`：任务栏宿主程序。
- `WindhawkMod`：任务栏嵌入模块、模块源码以及安装、检查和禁用脚本。
- `lightning.ico`：程序图标。
- `使用说明.md`：本说明。

安装包只包含 v3.15.3 的稳定主程序入口，不会包含任何旧版程序、历史 ZIP、构建缓存、日志、状态文件或个人设置。

首次启动后，程序会在自身目录创建或更新 `settings.json`、`cleaner.log` 和 `cleaner.state.json`。

## 4. 一键安装与启动

1. 双击 `MemoryCleanerFloat-Setup-v3.15.3.exe`。
2. 选择安装位置；有 D: 盘时默认是 `D:\MemoryCleanerFloat`，否则默认使用当前用户的本地程序目录。
3. 保持“创建桌面快捷方式”勾选，点击“立即安装”。
4. 安装器会自动安装本工具、Windhawk Portable 和任务栏模块，并启动主程序。
5. 看到“安装成功”后关闭安装器。首次解析系统符号可能需要几分钟，请保持联网。

## 5. 第一次使用示例

1. 启动主程序，确认托盘区出现闪电图标。
2. 左键单击工具面板，界面会显示“正在清理内存…”。
3. 正常情况下数秒后显示清理结果；慢速电脑仍保留 120 秒安全上限。
4. 右键打开菜单，可查看设置、日志和清理预览。

## 6. 主要操作

- 左键短按：执行一次内存清理；执行中会忽略重复点击。
- 右键：打开完整菜单。
- 按住左键横向拖动：调整任务栏内位置并保存。
- 悬浮窗口拖入任务栏：切换为任务栏嵌入模式。
- 托盘闪电图标：打开设置、日志、诊断或退出。
- 定时清理：在设置中配置 1–1440 分钟的间隔。

## 7. 配置、数据和读取范围

- 设置保存在程序目录的 `settings.json`。
- 运行日志保存在 `cleaner.log`，维护状态保存在 `cleaner.state.json`。
- 工具读取系统内存状态、当前会话进程、任务栏状态和网络流量计数。
- 仅对 `CloseProcessNames` 中明确列出的候选进程执行关闭；`KeepProcessNames` 中的项目受保护。
- 内存整理不等于释放驱动占用的非分页池；此类问题只诊断，不强制修改驱动或网卡。

## 8. 关闭、更新和恢复

- 关闭：从托盘菜单选择“退出”。若清理正在执行，程序会终止并释放该 worker。
- 更新：退出旧版后直接运行新版安装器并选择原安装目录；安装器会保留已有 `settings.json`。
- 恢复任务栏：运行 `WindhawkMod\禁用-Windhawk模块.ps1`，只禁用本模块，不删除 Windhawk 或其他模块。
- 卸载：打开 Windows“设置 → 应用 → 已安装的应用”，找到“内存释放任务栏工具”并卸载；也可运行安装目录内的 `Uninstall.exe`。卸载会禁用本工具的任务栏模块，但保留 Windhawk，避免影响其其他模块。

## 9. 常见问题

### 提示“清理启动失败”

确认 `MemoryCleanerFloat.ps1` 与 EXE 位于同一目录，且没有只复制主程序 EXE。

### 仍提示“清理超时”

先确认正在运行的是 v3.15.3。查看 `cleaner.log` 最后的 `cleanup step`：如果长时间停在某一步，说明是该系统调用或进程本身阻塞，不是已修复的输出管道死锁。退出程序会终止遗留 worker，之后可重试。

### 任务栏模块没有出现

检查系统是否为 Windows 11 x64、Windhawk 路径是否正确、是否正在首次解析系统符号，以及是否使用了第三方任务栏替换软件。也可运行 `WindhawkMod\检查任务栏模块.ps1`。

如果诊断中的 `Module version` 不是 `1.6.2`，说明仍在使用旧模块。退出工具后，直接重新运行 v3.15.3 安装器并选择原安装目录；无需删除原有 Windhawk。

### 清理后占用下降不明显

Windows 会按需重新使用可用内存；驱动非分页池、文件缓存或前台应用占用不一定能通过进程工作集整理释放。以日志中的 `before`、`after`、`trimmed` 和内核提示判断实际结果。

## 10. 联网、隐私和安全

- 内存清理、进程判断和日志记录均在本机完成，不上传进程列表或内存数据。
- 开启联网位置显示时，会向配置的公网位置服务查询当前公网 IP 所在国家或地区。
- Windhawk 首次加载模块时可能联网解析当前 Windows 构建对应的系统符号。
- 本工具不写入密码、Token 或账号凭据，不强制释放内核池，不自动停用网卡或修改驱动。
